/** Gemini vision model when the tenant `doc_converter` setting names none. */
export const DEFAULT_DOC_CONVERTER_GEMINI_MODEL = "google/gemini-2.5-flash";

/** Mistral OCR model when the tenant `doc_converter` setting names none. */
export const DEFAULT_DOC_CONVERTER_MISTRAL_MODEL = "mistral-ocr-latest";
