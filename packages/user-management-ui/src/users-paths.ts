/** Tenant users console — Settings overview entry, not a rail icon. */
export const USERS_PATH = "/settings/users";
/** Former admin-rail URL — keep redirects for bookmarks. */
export const USERS_LEGACY_PATH = "/admin/users";
