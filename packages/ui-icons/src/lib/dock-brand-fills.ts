/**
 * SVG `fill` values for primary-rail (dock) icons. Resolves to Ember tokens from
 * `packages/design-tokens/src/ember-primitives.css` wherever that stylesheet is loaded.
 */
export const dockBrandFill = {
  amber: "var(--amber)",
  cobalt: "var(--cobalt)",
  ember: "var(--ember)",
  emberStrong: "var(--ember-strong)",
  inkMuted: "var(--ink-3)",
  moss: "var(--moss)",
  rose: "var(--rose)",
} as const;
