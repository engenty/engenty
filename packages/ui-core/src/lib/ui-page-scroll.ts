/**
 * Main-area scroller (DESIGN.md). CSS lives in `ui-canvas-chrome.css`.
 * Pad with `pb-scroll-safe` when the node is already a scroller.
 */
export const uiPageScrollClassName = "ui-page-scroll" as const;
