# Screenshots for user/connect-slack

Drop PNGs here with these exact filenames — the doc
(`docs/content/user/connect-slack.md`) already references them as
`/images/connect-slack/<name>.png`. Capture at a normal desktop width
(~1280px), light theme, and crop to the relevant panel.

| File | What to capture |
| --- | --- |
| `01-connections-slack-card.png` | Engenty **Settings → Connections**, the **Slack** card with the **Connect** button (not connected state). |
| `03-slack-user-scopes.png` | Slack app **OAuth & Permissions → User Token Scopes** showing the eight scopes added. |
| `04-slack-redirect-url.png` | Slack app **OAuth & Permissions → Redirect URLs** with the `…/api/connections/oauth/callback` entry saved. |
| `05-slack-authorize.png` | Slack's **authorization / Allow** screen for the Engenty app (scope list + Allow button). |
| `06-slack-bridge-settings.png` | Engenty **Settings → Slack bridge** with channels listed and the Slack-channel picker / Bind buttons. |

`02` is intentionally unused (app-creation step needs no screenshot).
