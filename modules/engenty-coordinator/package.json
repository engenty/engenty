{
  "name": "@engenty/engenty-coordinator",
  "version": "0.0.1",
  "description": "Goal-driven orchestrator agent for Engenty — decomposes goals into tasks and monitors progress.",
  "private": true,
  "type": "module",
  "main": "./dist/src/plugin.js",
  "types": "./src/plugin.ts",
  "engenty": {
    "migrationsDir": "./supabase/migrations"
  },
  "exports": {
    ".": {
      "types": "./src/plugin.ts",
      "default": "./dist/src/plugin.js"
    },
    "./ai/registrar": {
      "types": "./ai/registrar.ts",
      "default": "./dist/ai/registrar.js"
    }
  },
  "scripts": {
    "build": "tsup src/plugin.ts ai/registrar.ts --format esm --clean",
    "dev": "tsup src/plugin.ts ai/registrar.ts --format esm --watch",
    "test": "vitest run --config vitest.config.ts",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@engenty/ai-core": "workspace:*",
    "@engenty/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
