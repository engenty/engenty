{
  "id": "secrets",
  "name": "Secrets Vault",
  "description": "Client-anchored secrets/password vault and paid-services registry; server-side encrypted, agent-grantable",
  "version": "0.0.1",
  "kind": "module",
  "provides": [
    "module.secrets",
    "module.secrets.read",
    "module.secrets.write",
    "ui.route.module.secrets"
  ],
  "capabilities": {
    "operations": true,
    "ai": false,
    "frontendTools": false,
    "ui": true
  },
  "env": {
    "feature": {
      "id": "secrets",
      "label": "Secrets vault (at-rest encryption)",
      "description": "Client-anchored secrets manager with server-side AES-256-GCM encryption",
      "recommended": true
    },
    "vars": [
      {
        "description": "AES-256-GCM key for secret payloads at rest (32 bytes, base64). Generate: engenty env generate (or openssl rand -base64 32). Phase 1 static key; superseded by per-tenant DEK + KMS in phase 2.",
        "feature": "secrets",
        "group": "Secrets",
        "key": "SECRETS_ENC_KEY",
        "obtain": { "kind": "generate", "generator": "base64-32" },
        "required": "optional",
        "scopes": ["root", "deploy"],
        "secret": true,
        "validate": "base64-32"
      }
    ]
  }
}
