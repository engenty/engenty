{
  "id": "connections-microsoft",
  "name": "Connections — Microsoft",
  "description": "Microsoft Graph connectors: Outlook mail + calendar and OneDrive files",
  "version": "0.0.1",
  "kind": "module",
  "provides": ["module.connections-microsoft"],
  "capabilities": {
    "operations": true,
    "ai": false,
    "frontendTools": false,
    "ui": false
  },
  "env": {
    "feature": {
      "id": "connections-microsoft",
      "label": "Microsoft connectors (Outlook, OneDrive)",
      "description": "OAuth app credentials for the Microsoft Graph connectors"
    },
    "vars": [
      {
        "description": "Application (client) ID of the Azure app registration used for the Microsoft Graph OAuth flow (tenant: common).",
        "feature": "connections-microsoft",
        "group": "Connections — Microsoft",
        "key": "MICROSOFT_OAUTH_CLIENT_ID",
        "obtain": {
          "kind": "provider",
          "instructions": [
            "1. Open the Azure Portal → App registrations and create a registration (supported account types: any org directory + personal Microsoft accounts).",
            "2. Add the Web redirect URI <ENGENTY_API_BASE_URL>/api/connections/oauth/callback.",
            "3. Under API permissions, add the delegated Microsoft Graph permissions: User.Read, Mail.Read, Mail.ReadWrite, Mail.Send, Calendars.Read, Calendars.ReadWrite, Files.Read, Files.ReadWrite.",
            "4. Copy the Application (client) ID from the Overview page."
          ],
          "url": "https://portal.azure.com"
        },
        "required": "optional",
        "scopes": ["root", "deploy"],
        "secret": false
      },
      {
        "description": "Client secret of the Azure app registration (Certificates & secrets → New client secret; copy the secret Value).",
        "feature": "connections-microsoft",
        "group": "Connections — Microsoft",
        "key": "MICROSOFT_OAUTH_CLIENT_SECRET",
        "obtain": {
          "kind": "provider",
          "instructions": [
            "In the same app registration, open Certificates & secrets → New client secret and copy the secret Value (not the ID)."
          ],
          "url": "https://portal.azure.com"
        },
        "required": "optional",
        "scopes": ["root", "deploy"],
        "secret": true
      }
    ]
  }
}
