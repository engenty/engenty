import type { EngentyPluginFactory } from "@engenty/plugin-sdk";

const copilotPlugin: EngentyPluginFactory = () => {
  // Copilot module: UI-only consumer of apps/ai for full-page chat.
};

export default copilotPlugin;
