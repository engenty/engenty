/**
 * Where the copilot's settings live: on its own desk, in the settings pane
 * (`?panel=manage`) — the same pane a specialist's desk opens. Nothing here
 * is a page of the module's: the copilot's conversation is the river
 * (`@engenty/ai-ui`'s `copilotRiverPath`), and its settings are the desk's.
 */
export const COPILOT_SETTINGS_PATH = "/copilot?panel=manage";
