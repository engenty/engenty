/** CLI commands for contacts module - placeholder. */
export function registerContactsCli(
  _program: { command: (name: string) => { description: (d: string) => void } },
  _dataDir: string
) {
  // TODO: add CLI commands if needed
}
