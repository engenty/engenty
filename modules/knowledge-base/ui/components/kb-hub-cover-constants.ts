/** Minimum height of the hub cover hero (topbar overlap + title block). */
export const KB_COVER_H = 260;
/** Compact height when no cover is set — just enough for topbar overlap + title row. */
export const KB_COVER_H_EMPTY = 132;

export const COVER_IMAGE_ACCEPT = "image/jpeg,image/png,image/webp,image/gif";
export const MAX_COVER_IMAGE_BYTES = 12 * 1024 * 1024;
